import 'package:flutter/material.dart';

class AppColors {
  // Colors for the app are defined here

  // primary color, only the button color is given
  static const primaryColor = Color(0xFFFF5700);

  static const backgroundGrey = Color(0xFFF6F6F6);

  // black and white colors are defined here for the case they should be
  // easy to change

  static const black = Colors.black;
  static const white = Colors.white;
}
